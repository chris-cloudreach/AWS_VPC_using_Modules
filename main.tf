module "module_network" {
  source = "./module_network"
  # private_c_cidr = var.private_c_cidr
  # # private_c_cidr = "${module.module_network}"
  # public_b_cidr = var.public_b_cidr
  # public_c_cidr = var.public_c_cidr
  # private_b_cidr = var.private_b_cidr
  # private_a_cidr = var.private_a_cidr
  # public_a_cidr = var.public_a_cidr
  # region = var.region
  # vpc_cidr = var.vpc_cidr
  # IG_name = var.IG_name
  # vpc_name = var.vpc_name
  
}